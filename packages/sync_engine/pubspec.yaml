name: sync_engine
description: "Sync engine: push/pull with seq cursors, meta/key channel, outbox (05). M4."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
