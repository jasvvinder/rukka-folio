name: core_crypto
description: "Crypto core: envelopes, key hierarchy, wrap/unwrap, sign/verify chain, fingerprints, Shamir (04). M3."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
