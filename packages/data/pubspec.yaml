name: data
description: "Local persistence: Drift + SQLCipher, projector (pure), snapshots, recompute (03). M2."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  core_ledger:
    path: ../core_ledger
  drift: ^2.34.4
  meta: ^1.16.0
  sqlite3: ^3.5.2

dev_dependencies:
  build_runner: ^2.16.1
  drift_dev: ^2.34.6
  lints: ^6.0.0
  test: ^1.25.0
