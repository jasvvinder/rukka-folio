name: core_ledger
description: "Ledger engine: entries, verbs to postings, invariants, amend/reverse, periods, balances (02). M1."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
