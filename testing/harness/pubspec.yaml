name: harness
description: "Two-client test rig: deterministic scheduler over a seeded, replayable network-reorder log (09 §1, ADR 2026-09-05i §7). Created at M2; devices + server join at M4."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
