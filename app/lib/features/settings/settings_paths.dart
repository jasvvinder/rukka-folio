/// Path constants for the Settings feature (features/README "Routes").
/// S13 is a root-navigator screen reached from Menu (S8) — not a tab root.
abstract final class SettingsPaths {
  /// S13 Settings hub.
  static const root = '/settings';
}
